# 皮肤工程方案设计 · 对齐 Project-X main 仓库

> 目标：把 Project-X 主仓库（E:/git/Project-X）已落地的皮肤系统**逐一对齐**到 X-exam 小程序——
> 复用其 Skin ID、维度模型、存储键名、令牌分层与选择/切换交互，并按小程序约束做工程适配，
> 同时**保留现有 8 个页面不动**（靠语义别名层承接）。
> 本文档为「方案设计」，尚未落地代码。实施分期见 §7。

---

## 0. 对齐基准（来自 Project-X main 仓库，已核实 · 以 origin/main 远端最新为准）

> ⚠️ **对齐基准澄清（2026-08-09 复核）**：本方案的皮肤契约以 **`origin/main`（远端最新提交）** 为准。本地 `E:/git/Project-X` 的 `main` 分支已落后于远端（`git fetch` 因代理被重置未能拉取），早期一度因读到本地过时 checkout 而误判"皮肤系统不存在 / 无默认皮肤"——**该误判已作废**。远端 `origin/main` 中皮肤系统完整落地，`SkinSwitcher.tsx` 明确 `export const DEFAULT_SKIN = "paper-edge"`，默认皮肤即**纸锋 paper-edge**（非"无默认"）。对齐前请确保本地 `git pull` 到最新 origin/main。

| 项 | main 仓库约定 | 来源 |
|----|--------------|------|
| Skin ID | `paper-edge`（纸锋，**默认**，`DEFAULT_SKIN='paper-edge'` 兜底）· `flat`（明澈 Flat 2.0） | `SkinSwitcher.tsx:40` `DEFAULT_SKIN` / `App.tsx:477` / `SKIN_OPTIONS` |
| 维度模型 | **皮肤（`data-skin`）** 与 **明暗（`data-theme`）正交**，互不干扰 | `SKIN-THEME.md` §一 |
| 存储键 | `projectx-skin`（持久）· `projectx-skin-chosen`（会话显式选择标记）· `projectx-skin-onboarded`（一次性引导标志） | `SkinSwitcher.tsx` / `SkinOnboarding.tsx` |
| 应用机制 | 写 `document.documentElement.dataset.skin/theme` → CSS `[data-skin="x"]` 变量覆盖块生效 | `SkinSwitcher.tsx` `writeLocalSkin` |
| 令牌分层 | L1 `:root`（基准=明澈 flat）/ L2 `[data-skin="x"]`（风格覆盖）/ L3 `[data-theme="dark"]`（暗色） | `src/apps/answer-card/client/theme/tokens.css`（运行生效；`design/tokens/tokens.css` 为设计源，二者同步） |
| JS 侧镜像 | `client/theme.ts` 导出 `tokens` 常量，供图表/canvas 引用（杜绝硬编码色） | `theme.ts` 头部注释 |
| 选择 UI（3 处） | ① 登录页 Palette 按钮（自管，未登录即可切）② 账号设置「外观/皮肤」区（受控）③ 首次强制引导层 `SkinOnboarding`（2 卡二选一） | `origin/main` `SkinSwitcher.tsx` / `SkinOnboarding.tsx`（**已发布**；触发条件：`projectx-skin-onboarded` 缺失才弹） |
| 控制模式 | `SkinSwitcher` 受控（skin/theme + onChange）/ 自管（直写 storage）双模式 | `SkinSwitcher.tsx` 注释 |
| 持久化层级 | 皮肤=**账号级**（`PATCH /api/users/me/settings {themeSkin}`）· 明暗=**设备级** | `SKIN-THEME.md` §二 |

---

## 1. X-exam 皮肤现状盘点

- `app.wxss` 已实现一套 editorial-brutalist 令牌，**变量名是短名**（`--paper/--blue/--ink/--err/--line` 等），且**实质就是 `paper-edge`（纸锋）风格**：纸米底 `#F1EFE9`、墨字 `#1A1917`、亮蓝 accent `#2E44FF`、直角硬影 `--shadow-hard: 8rpx 8rpx 0 ink`。
- 已带**兼容别名层**：`--brand/--text-1/--text-2/--text-3/--card-bg/--border/--success/--warning/--danger` 等指向短名（见 app.wxss 39–56 行），旧引用零改动即生效——这正是承载「换肤不破坏页面」的关键。
- `app.json` 的 `window` 与 `tabBar` 硬编码纸米底 `#F1EFE9` + 亮蓝选中 `#2E44FF`（= paper-edge 原生 chrome）。
- **缺口**：无 `flat`、无 `dark`、无切换入口、无首次引导层；canvas（trends/subjects）用硬编码色（非令牌）。

> 结论：X-exam 当前外观 = `paper-edge`。对齐 main 仓库时，paper-edge 基本是"现状保留 + 抽成 L2 覆盖块"；**明澈 `flat` 是新增皮肤**，且需补暗色维度。

---

## 2. 对齐原则（三条）

1. **契约对齐**：Skin ID、维度模型、存储键名、令牌分层、选择/切换交互结构，**全部沿用 main 仓库命名**，不另起炉灶（避免两套系统漂移）。
2. **小程序约束适配**：
   - `localStorage` → `wx.setStorageSync/getStorageSync`；`sessionStorage` → 小程序无此概念，**`projectx-skin-chosen` 直接并入 `wx.setStorageSync`**（语义降为"本会话/本机显式选择"，登出时清理即可，学生端无共享设备账号继承痛点，可简化）。
   - `document.documentElement.dataset` → 每页**根容器 view 的 `data-skin` / `data-theme` 属性**（WXSS 支持属性选择器，Skyline/WebView 均支持）。
   - 原生 chrome（导航栏背景字色、tabBar 颜色、页面背景）**无法靠 CSS 变量**改 → 用 `wx.setNavigationBarColor` / `wx.setTabBarStyle` / `wx.setBackgroundColor` 在 JS 层随皮肤联动。
   - 账号级持久化（`themeSkin` 落库）**降级为设备级**（若无后端支持）；若 `dl5zx.cn` 支持 `themeSkin` 字段，再接 `PATCH /api/users/me/settings` 对齐 main 仓库（见 §6）。
3. **零破坏**：保留现有短变量名（`--paper/--blue/--ink` 等）作为"语义别名层"，新增 `--px-*` L1/L2/L3 令牌，别名层改指 `--px-*`。页面 class 引用（`var(--blue)` 等）原样工作且自动随皮肤切换。

---

## 3. 令牌映射（核心，逐值对齐 tokens.css）

### 3.1 L1 基准（写在 `page` 选择器，等同 main 仓库 `:root`）
引入 `--px-*` 全套（品牌红阶/中性锌灰阶/状态色阶/图表8色/间距/圆角/字阶/阴影/动效），**同时让现有短别名指向它**：

```css
/* app.wxss · page { ... 在现有令牌后追加 } */
--px-brand: #C00F28;            /* 品牌绯红 */
--px-accent: #C00F28;           /* L1 基准(=flat 明澈) accent 用绯红；paper-edge 在 L2 翻为亮蓝 #2E44FF */
--px-ink: #1A1917;  --px-ink-soft: #4A4842;  --px-gray: #8B887E;
--px-paper: #F1EFE9; --px-paper-2: #E9E6DE;
--px-line: #D8D5CB;
--px-chart-1: #C00F28; --px-chart-2: #2563EB; --px-chart-3: #16A34A; ... --px-chart-8: #71717A;
/* 别名层改指 --px-*（页面零改动换肤） */
--blue: var(--px-accent);  --ink: var(--px-ink);  --paper: var(--px-paper);  --line: var(--px-line);
--brand: var(--px-accent); /* paper-edge 默认 accent=蓝，flat 时由 L2 翻回绯红 */
```

> px→rpx：间距/圆角/阴影按 `×2` 进 WXSS（如 md 圆角 8px→16rpx）；颜色值原样。

### 3.2 L2 风格覆盖（新增，对齐 main 仓库 `[data-skin="x"]`）

**`[data-skin="paper-edge"]`（= 现状，抽成覆盖块）**
```css
[data-skin="paper-edge"] {
  --px-paper: #F1EFE9; --px-paper-2: #E9E6DE; --px-ink: #1A1917; --px-ink-soft: #4A4842;
  --px-gray: #8B887E;  --px-line: #D8D5CB; --px-line-dark: #2E2E2B;
  --px-accent: #2E44FF; --px-accent-deep: #1F30C8; --px-accent-soft: rgba(46,68,255,0.09); --px-accent-bd: rgba(46,68,255,0.35);
  --px-err: #C00F28; --px-err-fg: #A10D22;
  --px-dark: #141413;
  --px-radius: 0;                 /* 直角纪律 */
  --px-shadow-hard: 8rpx 8rpx 0 var(--px-ink);
  --px-shadow-soft: none;
  --px-chart-1: #2E44FF; --px-chart-2: #8B887E; --px-chart-3: #C00F28; /* 蓝为主/红仅异常 */
}
```

**`[data-skin="flat"]`（明澈，新增）**
```css
[data-skin="flat"] {
  --px-paper: #F4F4F5; --px-paper-2: #E4E4E7; --px-ink: #18181B; --px-ink-soft: #52525B;
  --px-gray: #71717A;  --px-line: rgba(24,24,27,0.10);
  --px-accent: #C00F28; --px-accent-deep: #A10D22; --px-accent-soft: rgba(192,15,40,0.08); --px-accent-bd: rgba(192,15,40,0.35);
  --px-err: #C00F28; --px-err-fg: #A10D22;
  --px-dark: #0B0B0D;
  --px-radius: 16rpx;             /* 现代圆角（md 档） */
  --px-shadow-hard: 0 2rpx 8rpx rgba(192,15,40,0.18);
  --px-shadow-soft: 0 1rpx 2rpx rgba(24,24,27,0.05), 0 4rpx 12rpx rgba(24,24,27,0.06);
  --px-chart-1: #C00F28; --px-chart-2: #2563EB; ... --px-chart-8: #71717A; /* 完整8色板 */
}
```

### 3.3 L3 暗色（新增，两套皮肤各一套，精确值以 `tokens.css` 的 `[data-skin=*][data-theme="dark"]` 块为准实现时抄录）
```css
[data-skin="paper-edge"][data-theme="dark"] { --px-paper:#141413; --px-ink:#F1EFE9; --px-ink-soft:#C8C5BC; --px-line:rgba(241,239,233,0.08); ... }
[data-skin="flat"][data-theme="dark"]       { --px-paper:#0B0B0D; --px-ink:#FAFAFA; --px-ink-soft:#A1A1AA; --px-line:rgba(250,250,250,0.10); ... }
```

> 实现时 dark 块逐字段对齐 `tokens.css`（对比度 ≥4.5:1 已在其内审过），本文档给出原则与关键值，不臆造。

---

## 4. 小程序专属适配（main 仓库无此层，必须补）

| 适配点 | 方案 |
|--------|------|
| **根容器挂载** | 每个页面（login 除外，可全屏引导）最外层 `<view class="container" data-skin="{{skin}}" data-theme="{{theme}}">`，`[data-skin]` 选择器在其子树内覆盖变量 |
| **全局状态** | `app.js` `globalData.skin/theme` 由 `wx.getStorageSync('projectx-skin')` 初始化；`onLaunch` 预置，防白闪（对齐 main 仓库 `main.tsx` 渲染前预置 `data-skin`） |
| **原生 chrome 联动** | 切换时调 `wx.setNavigationBarColor({frontColor, backgroundColor})` + `wx.setBackgroundColor({backgroundColor})` + `wx.setTabBarStyle({color, selectedColor, backgroundColor, borderStyle})`，颜色按当前 skin/theme 取（对应 `app.json` 现硬编码值的动态版） |
| **持久化封装** | 新建 `utils/theme.js`：`getSkin()/setSkin()/getTheme()/setTheme()/markOnboarded()/shouldOnboard()`，键名**严格对齐** `projectx-skin`/`projectx-skin-chosen`/`projectx-skin-onboarded` |
| **JS 侧令牌镜像** | `utils/theme.js` 导出 `SKIN_TOKENS`（仿 main 仓库 `theme.ts`）：`{ paper-edge:{ chart:[...], accent:'#2E44FF', ... }, flat:{...} }`，供给 canvas（trends/subjects）按当前 skin 取色，杜绝硬编码 |
| **控制模式** | 对齐 `SkinSwitcher`：受控（page data `skin/theme` + `onChange`）/ 自管（`utils/theme` 直写 storage + 设 data-skin）双模式 |

---

## 5. 选择 / 切换 UI（对齐 main 仓库三入口）

| 入口 | 对齐对象 | 实现 |
|------|----------|------|
| **A · 登录页 Palette（自管）** | main 仓库登录页右上角 Palette | `pages/login` 右上角加图标按钮 → 弹 skin + theme 选择；未登录即可切，写 `wx.storage` + 设 `data-skin` |
| **B · 设置页（受控）** | main 仓库「账号设置 → 外观/皮肤」 | `pages/profile` 加「外观 / 皮肤」区：明度 SegmentedControl + 两皮肤 Radio，即时生效 |
| **C · 首次强制引导层** | main 仓库 `SkinOnboarding`（已确认在 `origin/main` 发布） | 首进（`shouldOnboard()` = `onboarded` 缺失）弹全屏 2 卡（明澈/纸锋预览 + 简介），**必须二选一**，确认写 `chosen`+`skin`+`onboarded`；**注意：`DEFAULT_SKIN='paper-edge'` 仅作兜底，首次用户走强制选择，实际不触达默认** |

- skin 选项注册表（对齐 `SKIN_OPTIONS`）：`[{id:'paper-edge',label:'纸锋 Paper Edge'},{id:'flat',label:'明澈 Flat 2.0'}]`，新增皮肤时在此登记。
- 默认皮肤对齐 main 仓库 = `paper-edge`（= 现状，零视觉变化，风险最低）。

---

## 6. 数据流（对齐 + 降级）

```
切换皮肤 ──▶ page state (skin/theme)
              │
              ├─▶ utils/theme.setSkin/setTheme  写 wx.storage(projectx-skin / projectx-skin-chosen)
              ├─▶ 根容器 data-skin/data-theme 即时生效（CSS 覆盖）
              ├─▶ wx.setNavigationBarColor / setTabBarStyle / setBackgroundColor（原生 chrome）
              ├─▶ canvas 重绘（SKIN_TOKENS 按 skin 取色）
              │
              └─▶ 【可选·账号级】已登录且后端支持 → PATCH /api/users/me/settings {themeSkin}
                      GET /api/auth/me 下发 themeSkin（换设备恢复）；不支持则仅设备级
```

> X-exam 是学生端，共享设备账号继承痛点弱，`projectx-skin-chosen` 简化为本机显式标记（登出清理）。是否接账号级取决于 `dl5zx.cn` 是否支持 `themeSkin` 字段——**待确认**（见 §9）。

---

## 7. 实施分期（落地计划，非本次执行）

| Phase | 内容 | 产出 | 风险 |
|-------|------|------|------|
| **0 令牌重构** | `app.wxss` 引入 `--px-*` L1/L2/L3，别名层改指 `--px-*`；`[data-skin="paper-edge"]` 抽成覆盖块（视觉不变） | 纯样式重构，8 页零改动 | 需逐个核对 class 引用别名正确 |
| **1 主题基建** | 新建 `utils/theme.js`；`app.js` 全局 skin/theme + onLaunch 预置；各页根容器接 `data-skin/data-theme` | 基础设施 | 根容器接入需改 8 个 wxml |
| **2 flat 皮肤** | `[data-skin="flat"]` + 两套 dark 覆盖块（对齐 tokens.css dark） | 明澈皮肤可用 | dark 值需逐字对齐，避免对比度不达标 |
| **3 原生 chrome 换肤** | navBar/tabBar/backgroundColor 随 skin/theme 联动 | 原生栏跟随 | `app.json` 现硬编码值需抽出动态版 |
| **4 选择/切换 UI** | profile 切换器 + login Palette + 首次引导层 | 三入口对齐 main 仓库 | 引导层可能对学生端偏重，待确认 |
| **5 canvas 接令牌** | trends/subjects 改读 `SKIN_TOKENS` | 图表随皮肤换色 | 需重测图表动画 |
| **6 实测对照** | DevTools 双皮肤×双主题 + 真机预览 | 验收 | Skyline/WebView 兼容复核 |

---

## 8. 风险与注意

- **Skyline vs WebView**：`[data-skin]` 属性选择器两者都支持；但 X-exam 配置里 Skyline 开关与 `app.json` 未声明 `renderer` 矛盾（旧 P1）。Phase 0–2 先按 **WebView** 验证，真上 Skyline 再复核（尤其 `cover-view`/canvas 动画）。
- **tabBar 图标**：当前 tabBar 仅文字无 icon，换肤只需 `setTabBarStyle` 改色；若后续加 icon，需为每皮肤每主题备不同色资源。
- **包体积**：新增令牌块 + `utils/theme.js` < 10KB，无影响（主包远低于 2MB 上限）。
- **别名层正确性是零破坏的前提**：Phase 0 必须确认所有页面 class 走的是别名（`var(--blue)` 等）而非裸色值；裸色值会"换肤不生效"。

---

## 9. 待你拍板（对齐前需确认 3 项）

1. **默认皮肤**：对齐 main 仓库用 `paper-edge`（= 现状，零变化）✅ 推荐——已核对 origin/main `DEFAULT_SKIN='paper-edge'` 铁证。还是另定？
2. **账号级持久化**：`dl5zx.cn` 后端是否支持 `themeSkin` 字段？不支持则皮肤仅设备级（不影响对齐前端机制）。
3. **首次强制引导层（入口 C）**：对齐 main 仓库要做，但学生端可能嫌啰嗦——做 / 不做 / 改为"设置里可改、首进不强弹"？

---

## 附：与 main 仓库的差异（小程序约束导致，非漂移）

| main 仓库 | X-exam 小程序 | 原因 |
|-----------|--------------|------|
| `document.documentElement.dataset` | 根容器 `data-skin` 属性 | 小程序无 document |
| `localStorage` + `sessionStorage` | `wx.setStorageSync`（合并 chosen） | 小程序无 sessionStorage |
| 账号级 `themeSkin` 落库 | 设备级（可降级） | 学生端共享设备痛点弱 |
| 原生 navBar 靠 CSS 变量 | `wx.setNavigationBarColor` 等 API | 原生 chrome 不吃 CSS 变量 |
| `client/theme.ts` | `utils/theme.js`（SKIN_TOKENS） | 目录约定不同 |
