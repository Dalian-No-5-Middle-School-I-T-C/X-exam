# 2026-08-09 工作日志

## 皮肤工程方案设计（对齐 Project-X main 仓库）
- 探查 E:/git/Project-X 主仓库皮肤系统真相源：`src/apps/answer-card/client/{theme.ts, SkinSwitcher.tsx, SkinOnboarding.tsx}`、`design/tokens/tokens.css`、`readus/SKIN-THEME.md`、`.workbuddy/qa-skin-onboarding-report.md`。
- 对齐契约已确认：Skin ID `paper-edge`（默认/纸锋）· `flat`（明澈）；维度 `data-skin`×`data-theme` 正交；存储键 `projectx-skin`/`projectx-skin-chosen`/`projectx-skin-onboarded`；令牌 L1 `:root`→L2 `[data-skin=x]`→L3 `[data-theme=dark]`；JS 镜像 `theme.ts`；三入口（登录 Palette / 设置页 / 首次引导层）。
- X-exam 现状：app.wxss 现有短变量名（`--paper/--blue/--ink` 等）+ 别名层，实质就是 paper-edge 风格；无 flat/dark/切换入口；canvas 硬编码色。
- 产出：`皮肤工程方案设计.md`（完整方案 + 令牌映射 + 小程序适配 + 6 期实施计划 + 3 项待拍板）。CHANGELOG 同日段追加"计划项/待办"占位。
- 注意：X-exam 小程序约束 → 无 document（根容器 data-skin 属性挂载）、无 sessionStorage（合并进 wx.storage）、原生 chrome 用 setNavigationBarColor/setTabBarStyle API、账号级 themeSkin 可降级设备级。

## 环境/网络
- 用户之前反馈 WorkBuddy 不开梯子报 3002、开梯子报 502；根因是 `*.tencent.com` 未进 NO_PROXY，被误塞入梯子代理（127.0.0.1:7890）。已生成 `C:/Users/杨钊霖/AppData/Local/Temp/fix_noproxy.reg` 双击补丁（用户侧执行，沙箱禁 setx/reg 系统工具）。

## 对齐基准纠错（2026-08-09 晚 · 用户质疑触发）
- **关键纠正**：此前"探查主仓库"读的是**本地过时 checkout**（本地 `main` 落后 `origin/main`，`git fetch` 被梯子 reset 未能拉取），一度误判"皮肤系统不存在 / 无默认皮肤"。经 `git show origin/main:...` 核实远端最新：皮肤系统完整落地，`SkinSwitcher.tsx:40` 明确 `export const DEFAULT_SKIN = "paper-edge"` → **默认皮肤=纸锋 paper-edge（非"无默认"）**。用户的"没有默认皮肤"是误记（源于 QA 报告"首次强制二选一引导"的误导），但质疑触发了正确复核。
- **真实令牌位置**：`src/apps/answer-card/client/theme/tokens.css`（运行生效；`design/tokens/tokens.css` 仅设计源，二者同步）。`:root`=flat 基准（白底绯红，chart-1=#C00F28）；`[data-skin="paper-edge"]`=L2 覆盖（纸面墨蓝、直角 radius 全 0、硬影 `8px 8px 0`、accent=#2E44FF、success/info=蓝、danger=红、chart 板墨蓝+红领衔）。**X-exam 现状即 paper-edge 风格 → 默认零破坏**。
- **已同步修正**：`皮肤工程方案设计.md` §0 加"对齐基准澄清（origin/main 为准）"+ 修正令牌路径/`DEFAULT_SKIN` 出处/L1 accent 值；`CHANGELOG.md` 同日段"计划项"补澄清、待拍板第1条改为"已确认=paper-edge"。
- **剩余待拍板**：① 后端 `dl5zx.cn` 是否支持 `themeSkin` 字段；② 是否做首次强制引导层（对齐 main 仓库要做，但学生端可能嫌啰嗦）。
- **行动项**：对齐前需用户本地 `git pull` Project-X 到最新 `origin/main`（梯子需放行 github.com）。

## 强制引导层 vs 默认 机制澄清（2026-08-09 再一轮 · 用户质疑触发）
- **用户质疑点**："强制选择，那默认永远不生效？做强制弹层，不清楚"。
- **远端 origin/main 逐行核实结论**：
  - `SkinOnboarding.tsx` **已在 origin/main 发布**（早前 QA 报告"未提交"是本地工作树过时视角，已作废）。
  - 触发条件 `shouldShowSkinOnboarding()`（:40-43）= `localStorage.projectx-skin-onboarded !== "1"` → **仅 onboarded 缺失才弹**（首进/清缓存必弹）。
  - 确认 `handleConfirm()`（:57-66）同时写 `sessionStorage[SKIN_CHOSEN_KEY]` + `writeLocalSkin(selected)`（→ 落盘 `projectx-skin` + 设 `data-skin`）+ `localStorage[SKIN_ONBOARDED_KEY]="1"` → **两个标志一起写，不留脏状态**。
  - `App.tsx:477` `localStorage.getItem("projectx-skin") || DEFAULT_SKIN` → **默认仅兜底**：① 存储读异常 catch；② 极端脏状态；③ CSS `:root`（flat）根兜底（JS 总显式设 data-skin，flat 仅 JS 失效时出现）。
- **定性**：首次用户走强制选择不触达默认；默认 = 防御性兜底 + 单一真相源（`DEFAULT_SKIN` 改一处全局改默认）。用户"默认永远不生效"对首屏成立，但默认非死代码。
- **X-exam 决策**：对齐 main 则做强制弹层；但学生端首开加"选皮肤"步有摩擦，可降级为"设置里可改、首进不强弹"。已把方案文档/CHANGELOG 的"强制引导层"来源与兜底定性补严谨。

## 皮肤工程落地（方案 B · 2026-08-09 晚 · 用户选 B"开始"）
- 用户拍板方案 B（轻量对齐：做 login 调色板 + profile 外观切换，不做首次强制引导层）。
- 已实现：① `utils/theme.js`（契约对齐 projectx-* 键、DEFAULT_SKIN=paper-edge、chartTokens 镜像）；② `app.js` globalData.skin/theme + applyTheme/applyChrome（导航栏/tabBar 用 wx API 联动）；③ `app.wxss` 保留 paper-edge 基准，新增 flat(明澈)/dark/组合 覆盖块 + .app-root 铺满屏高 + 切换器样式；④ 8 页根容器挂 app-root+data-skin/data-theme，各页 onShow 调 syncPage（detail/leaderboard 补 onShow）；⑤ profile「外观」区 + login 调色板。
- 校验：10 个 JS 文件 `node --check` 全过；8 页 wxml 均含 data-skin/data-theme。
- 关键机制：纸锋=现状（零破坏）；明澈 flat 用绯红品牌+绿 success+柔和阴影，与纸锋语义映射不同（已显式重定义别名层）；暗色分 paper-edge-dark 与 flat-dark 两组合。
- 待办（未完）：① canvas(trends/subjects) 硬编码色未接 chartTokens，切皮肤图表色不跟随；② 后端 themeSkin 字段未知。
- 注意：编辑期多次遇"File has been modified since read / EBUSY"——疑似 WorkBuddy 后台 linter/formatter 实时改写文件，导致部分 Edit 首轮失败，重读后重试即成功；非代码问题。

## 皮肤工程收尾（canvas 接令牌 · 2026-08-09 深夜）
- 补齐最后一块：trends/subjects 的 canvas 折线/雷达/柱状/标签颜色由 `theme.chartTokens(skin, theme)` 取色（之前硬编码纸锋色 `#D8D5CB/#8B887E/#2E44FF` 等）。
- 改法：`drawLine/drawRadar/drawBar` 加 `tk` 参数；页面 `drawLine/drawAll` 内 `getApp()` 取 globalData.skin/theme 算 tk 并传入；rAF 每帧重绘读最新 tk（切皮肤后 onShow→load→重绘即跟随）。
- 雷达填充透明度：`poly()` 改用 `ctx.globalAlpha=0.12` + `ctx.fillStyle=color` 替代写死 `rgba(...)`，消除硬编码 rgba。
- 校验：trends/subjects/theme 三个 JS `node --check` 全过；grep 确认无 `#D8D5CB/#8B887E/#2E44FF/#1A1917/#C00F28/rgba(139/rgba(46` 硬编码残留。
- CHANGELOG 皮肤段从"待办"提升为"已落地"+canvas 收尾条目；剩余：真机验证、后端 themeSkin、强制引导层（方案 B 不做）、Skyline 定调。
- 皮肤工程主体（契约/全局态/样式/挂载/双入口/canvas）至此全部完成，方案 B 收尾。

## 绝对严格 QA 验证 + 修复闭环（2026-08-09 收尾后 · 用户"QA验证，绝对严格"）
- 主理人建团队 `software-qa-skin`（齐活林/软件团队 SOP），派 QA 严过关做绝对严格验证，再派工程师寇豆码修缺陷，最后 QA 回归。
- **QA 首轮发现**：① D1 P1 — `pages/profile/profile.wxml:2` 根容器仅为 `<view class="container">`，缺 `app-root`+`data-skin`+`data-theme` → profile 页切皮肤/暗色整页不重绘（该页恰是"外观"入口）；② D2 P1 — `app.js` applyChrome 仅调 setNavigationBarColor+setTabBarStyle，**漏 wx.setBackgroundColor**，与 CHANGELOG:54 及设计文档不符 → 下拉/overscroll 背景不随明暗联动；③ D5 一致性 — profile.js onShow 未用 syncPage 而手动 setData。
- **PASS 项（首轮）**：theme.js 单测 32/32 全绿（桩 wx+getApp 真跑）；P0 安全回归（全仓 grep 无 token 进 URL、detail.js 走 downloadFile+header Authorization、request.js 无 getBuffer）；trends/subjects canvas 无硬编码色；10 个 JS `node --check` 全过；app.wxss 四块覆盖选择器齐全。Minor：D3 切换器 `color="#2E44FF"` 硬编码、D4 CHANGELOG 命名小误。
- **修复（工程师寇豆码）**：D1 profile.wxml:2 改 `<view class="app-root container" data-skin="{{skin}}" data-theme="{{theme}}">`；D2 app.js applyChrome 补 `wx.setBackgroundColor({ backgroundColor: navBg, ... })`（navBg 按 skin/theme 取）；D5 profile.js onShow 改 `theme.syncPage(this)` 并删手动赋值/`const app=getApp()`。
- **QA 回归**：D1/D2/D5 全部闭环 PASS；复检 node --check 仍过、theme.js 单测仍 32/32、P0 安全与 canvas 无回归、8 页根容器均在；**最终 verdict：源码无 P1 缺陷，可交付**。Minor(D3/D4) 不阻塞。
- 结论：皮肤工程经"绝对严格"QA 后，原 3 处源码缺陷清零，剩余仅非阻塞 Minor（切换器硬编码色、CHANGELOG 命名）。团队已优雅解散。

## Git 合并冲突解决（2026-08-09 深夜 · 用户"以远端最新main为准，修完自动提交"）
- 分支 `UI方案回归WEB端` 有一个进行中 merge（`.git/AUTO_MERGE` 存在、`MERGE_HEAD` 缺失，索引 8 文件 stage 1/2/3 未合并）。
- 8 个冲突文件（`CHANGELOG.md` `app.js` `detail.js` `login.js` `scores.js` `semester.js` `subjects.js` `trends.js`）= both modified。
- 用户裁定：冲突以 **远端最新 origin/main（34b8202）为准**。
- 处理：`git fetch origin` 后逐文件 `git checkout origin/main -- <f>` + `git add` 解除 8 处冲突；非冲突已暂存改动（app.wxss、各页 wxml 挂载、utils/theme.js、皮肤工程方案设计.md、pr1/pr2.diff、记忆文件）保留；重建 `MERGE_HEAD=origin/main` 后 `git commit` 为真正 merge commit `5f47138`，清理 AUTO_MERGE，工作树干净。
- 验证：8 文件 `git diff --quiet origin/main` 全部 MATCH（逐字节一致）；7 个 JS `node --check` 全过。
- 注意/main 的真相副作用：main 删了 `pages/change-password/*` 与 `utils/response.js`，随 merge 一起删除（用户选"以 main 为准"，属预期）。
- **一致性风险（待用户验证）**：8 个冲突文件现已回退为 main 版本，但本分支保留的 app.wxss/wxml 挂载/utils/theme.js 仍是我们皮肤的 CSS/工具层。若 main 的 app.js 未调用 `theme.syncPage` 或未设 globalData.skin/theme，则 wxml 的 `data-skin/data-theme` 可能取不到值、CSS 变量覆盖块不命中。建议微信开发者工具导入验证换肤是否仍生效，必要时再决定是"以本分支皮肤 JS 为准"还是"对齐 main 的皮肤实现"。当前未 push（本地领先 origin/UI方案回归WEB端 25 commits）。
